## env kurulumu

`.env.example` dosyasini yerelde `.env` olarak kopyalayin. Gercek API anahtarini yalnizca yerel `.env` dosyasinda tutun.

PowerShell:

`Copy-Item .env.example .env`

Ardindan `.env` icindeki `AI_ANAHTARI=` satirina kendi anahtarinizi yazin.
